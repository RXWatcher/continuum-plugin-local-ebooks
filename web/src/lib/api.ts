// Stub for SPA scaffold — will be replaced in Task 10 (web API client).

/** Returns the cached Continuum theme string, or null if not yet loaded. */
export function getCachedTheme(): string | null {
  return null;
}
